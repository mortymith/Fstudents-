# Core Dependencies
fastapi[standard]==0.115.0    # FastAPI web framework with recommended extras (docs, dev tools, security)
pydantic==2.9.2              # Data validation and settings using Python type annotations
pydantic-settings==2.1.0     # Structured application settings built on Pydantic
pydantic[email]

# SQLAlchemy 2.0 with async support
sqlalchemy==2.0.23           # SQL toolkit and ORM for Python (core sync functionality)
sqlalchemy[asyncio]==2.0.23  # Async engine/session helpers for SQLAlchemy 2.0
redis==5.0.8                 # redis driver with async support 

# Database Drivers
asyncpg==0.29.0              # High-performance asyncio PostgreSQL driver

# Testing Framework
pytest==9.0.1                # Main testing framework for Python
pytest-asyncio==1.3.0        # asyncio support for pytest (testing async code)
pytest-cov==4.1.0            # Coverage plugin for pytest
pytest-xdist==3.5.0          # Run tests in parallel across CPUs
pytest-benchmark==4.0.0      # Benchmarking tools for measuring performance in tests
pytest-mock==3.12.0          # Thin wrapper around unittest.mock for pytest fixtures

# Test Data Factories
factory-boy==3.3.0           # Fixtures replacement to create test objects/instances
Faker==38.2.0                # Generate fake data (names, addresses, etc.) for tests and seeding


# Development Tools
ipython==8.18.1              # Enhanced interactive Python shell
python-dotenv==1.0.0         # Load environment variables from .env files
watchfiles==0.21.0           # File watching utility for auto-reload in development

# Async Utilities
anyio==4.0.0                 # Async compatibility layer across asyncio/trio backends
httpx==0.25.1                # Async-capable HTTP client (sync and async APIs)

# Security
python-jose[cryptography]==3.4.0  # JOSE/JWT implementation (with cryptography backend)
argon2-cffi==25.1.0               # Argon2 password hashing bindings for secure password storage
cryptography==41.0.7              # Cryptographic primitives and recipes

# Logging
structlog==24.4.0           # Structured logging library, easier event-based logging

# API Documentation (optional)
python-multipart==0.0.7      # Multipart form parser for file uploads (used by FastAPI)
uvicorn[standard]==0.30.6    # ASGI server for FastAPI (with performance extras like uvloop)
